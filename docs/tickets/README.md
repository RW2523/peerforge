# Execution Tickets (Arinar V2)

This folder contains the canonical build tickets (specs) for Cursor to implement.

Rules:
- Share **one ticket at a time** with Cursor.
- Cursor must produce a report under `arinar-v2/reports/tickets/` for each ticket.
- `make verify` is the canonical gate unless the ticket explicitly states otherwise.
- No duplicate systems: follow `arinar-v2/docs/design/DECISIONS-SOURCE-OF-TRUTH-2026-02-09.md`.

